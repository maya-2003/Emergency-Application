<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EmergencyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/grayy</item>
        <item name="android:navigationBarColor">@color/gray_light</item>
    </style>

    <style name="Theme.EmergencyApplication" parent="Base.Theme.EmergencyApplication" />
</resources>